# Core Ollama integration
ollama

# Voice Synthesis (TTS)
gTTS
edge-tts

# Speech Recognition & Transcription (STT)
SpeechRecognition
faster-whisper
pyaudio

# Wake-word Spotting (KWS)
sherpa-onnx
pvrecorder
numpy

# GUI Dashboard & Overlays
PyQt5

# Internet / web tools
requests
ddgs
trafilatura
sqlite-vec

# Optional browser automation (opt-in: set browser_enabled=true in config.json;
# also run 'playwright install chromium' once)
# playwright

# MCP client/server SDK (mcp_servers in config.json)
mcp

# Terminal access (persistent shell via cleat)
cleat

# System & Diagnostics skills
psutil
